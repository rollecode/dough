// Deep-linkable filtered budget view, e.g. /budget/overspent. Renders the same budget page, which
// reads the filter (overspent, underfunded, available) from the URL path.
export { default } from "../page";
