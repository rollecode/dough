// Deep-linkable account view, e.g. /transactions/veeran-tili. Renders the same transactions page,
// which reads the account slug from the URL and pre-selects that account's filter.
export { default } from "../page";
